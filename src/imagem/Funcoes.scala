package imagem
import modelo._

object Funcoes {
	def geraImagem(s: List[ElemSilhueta], nomeArq: String): Unit = throw new RuntimeException
	def preencheRetangulo(a: Matriz[Int], 
		      lin1: Int, lin2: Int,
		      col1: Int, col2: Int, k: Int): Unit = throw new RuntimeException
}
